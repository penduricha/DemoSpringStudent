spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/TMANewDataSrc
    username: root
    password: 123
    driver-class-name: org.mariadb.jdbc.Driver
#    port la 8080
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
